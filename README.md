Web-based application (HTML/CSS/JS + backend)

Role-based login: Admin / User

No advanced UI framework required (basic formatting acceptable)

Database-driven (MySQL / PostgreSQL / MongoDB – interchangeable)

Maintenance module = master data management

Reports are read-only

Transactions modify data

Fine calculation logic assumed:

Fine applies if return date > expected return date
# -Library-Management-System
